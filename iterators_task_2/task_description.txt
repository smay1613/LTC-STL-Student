You must implement output iterator adaptor that should work with different types of containers (that support insert operation) and support move semantics.

Example: vector<string> {“1”, “2”, “3”} -> string {“123”}
